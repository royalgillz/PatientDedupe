# Copy to .env for local development. The hosted demo sets DATABASE_URL as a secret
# (a Supabase connection string). Never commit a real connection string.
DATABASE_URL=postgres://postgres:postgres@localhost:5433/patientdedupe
PORT=8787
